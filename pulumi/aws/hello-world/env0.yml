version: 1

deploy:
  steps:
    setupVariables:
      before:
        - rm -rf /opt/pulumi
      after:
        - npm i
        - curl
        - ls /opt
        - ls /
        - ls .
        - pwd

destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd