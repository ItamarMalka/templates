version: 1

deploy:
  steps:
    setupVariables:
      after:
        - rm -rf /opt/pulumi
        - npm i
        - curl
        - ls /opt
        - ls /
        - ls .
        - pwd

destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - ls
        - ls /opt
        - ls /
        - ls .
        - pwd